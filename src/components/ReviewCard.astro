---
export interface Props {
  outerClass?: string;
}

const { outerClass } = Astro.props;
---

<div
  class={`w-full flex flex-col overflow-hidden rounded-2xl border border-gray-200 bg-white ${outerClass}`}
>
  <div class="flex flex-col gap-2 p-8 justify-between text-xl">
    <slot />
  </div>
</div>
