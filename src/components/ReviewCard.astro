---
export interface Props {
  outerClass?: string;
}

const { outerClass } = Astro.props;
---

<div
  class={`w-full flex flex-col overflow-hidden rounded-lg border border-gray-100 bg-white ${outerClass}`}
>
  <div class="flex flex-col gap-2 p-8 justify-between text-xl text-zinc-600">
    <slot />
  </div>
</div>
